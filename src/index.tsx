import * as React from 'react';
import * as ReactDOM from 'react-dom';
import TaskDashboard from './TaskDashboard';

ReactDOM.render(<TaskDashboard  />, document.getElementById('root') as HTMLElement);
